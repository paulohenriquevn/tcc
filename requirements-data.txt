# Lightweight data pipeline dependencies.
# Used by: accents_pt_br_dataset.ipynb
# The full requirements.txt includes this file via -r.
# Pin ALL versions for reproducibility. Never use >= or ~=.
# Update date: 2026-02-26

# Core (torch/torchaudio: use Colab pre-installed or install separately)
numpy==1.26.4
scipy==1.14.1
scikit-learn==1.6.1

# Audio I/O (needed by cv_manifest_builder on cache miss)
soundfile==0.13.1

# Dataset download and publication (HuggingFace)
datasets==3.3.2
huggingface-hub==0.28.1

# Data and analysis
pandas==2.2.3
pyarrow==18.1.0

# Visualization
matplotlib==3.10.0
seaborn==0.13.2

# Progress bars
tqdm==4.67.1

# Config
pyyaml==6.0.2
