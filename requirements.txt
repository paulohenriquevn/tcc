# Stage 1.5 — Latent Separability Audit (full ML stack)
# Pin ALL versions for reproducibility. Never use >= or ~=.
# Update date: 2026-02-26

# Lightweight data deps (shared with dataset notebook)
-r requirements-data.txt

# Core ML (Colab pre-installs torch — these pins ensure reproducibility)
torch==2.6.0
torchaudio==2.6.0

# Audio processing
librosa==0.10.2.post1
pyloudnorm==0.1.1

# Speaker embeddings (ECAPA-TDNN)
speechbrain==1.0.2

# SSL features (WavLM) + Backbone (Qwen3-TTS)
# transformers pinned to 4.57.3: required by qwen-tts==0.1.1 (hard dep).
# Compatible with WavLM AutoModel (supported since transformers ~4.12).
transformers==4.57.3
accelerate==1.12.0

# Backbone (Qwen3-TTS) — requires GPU, Colab only
qwen-tts==0.1.1

# Experiment tracking (optional, for Stage 2+)
# wandb==0.19.6

# Testing
pytest==8.3.4
pytest-cov==6.0.0
